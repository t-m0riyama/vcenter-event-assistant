services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////data/vea.db
    volumes:
      - vea_data:/data

volumes:
  vea_data:
